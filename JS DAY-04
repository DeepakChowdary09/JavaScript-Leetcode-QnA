/**
 * LeetCode 2665: Counter II
 *
 * Explanation:
 * Build a counter with three methods that share the same captured state.
 * - increment(): ++curr
 * - decrement(): --curr
 * - reset(): curr = init
 * Uses a closure to keep `curr` private and persistent across calls.
 *
 * Time Complexity: O(1) per operation
 * Space Complexity: O(1)
 */

/**
 * @param {number} init
 * @return {{increment: Function, decrement: Function, reset: Function}}
 */
var createCounter = function(init) {
  let curr = init;
  return {
    increment: function() {
      return ++curr;
    },
    decrement: function() {
      return --curr;
    },
    reset: function() {
      curr = init;
      return curr;
    }
  };
};

/** Example usage:
 * const counter = createCounter(5);
 * counter.increment(); // 6
 * counter.increment(); // 7
 * counter.decrement(); // 6
 * counter.reset();     // 5
 */
